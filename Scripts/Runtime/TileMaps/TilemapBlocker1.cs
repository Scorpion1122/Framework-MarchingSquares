using UnityEngine;
using UnityEngine.Tilemaps;

namespace Thijs.Framework.MarchingSquares
{
    public class TilemapBlocker : MonoBehaviour
    {
        [SerializeField] private Tilemap[] tilemaps;

        public void InitializeChunkData(TileTerrain grid, ChunkData chunk)
        {
            //UnityEngine.Tilemaps.Tilemap map;
            //map.GetTilesBlock();
            //map.cellSize;
        }
    }
}
